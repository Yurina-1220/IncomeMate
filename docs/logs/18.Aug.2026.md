# 2026年8月18日 作業ログ

## 作業対象

- バージョン目安：`v0.1.0-dev`
- ブランチ：`main`
- 対象：Supabase接続とPC画面の保存確認

## 実施内容

- `.env`の`SUPABASE_URL`を確認した
- API URL形式になっていた`/rest/v1`をURL末尾から削除した
- IncomeMateをローカルで起動した
- ローカルデモログインでPC画面を確認した
- Supabaseからテスト識別子の読み込みを確認した
- テスト用の最小データを保存した
- ページを再読み込みし、保存データが残ることを確認した
- テスト値`1`を画面から入力し、再読み込み後も残ることを確認した
- テスト値を`0`へ戻し、元の空欄と合計金額に戻ることを確認した

## 結果

- Supabase読み込み：成功（HTTP 200）
- Supabase保存：成功（HTTP 200）
- PC画面からの保存・読み込み：成功
- ビルド：成功
- `.env`：GitHubへ保存していない
- 一時テスト用のマーカー：除去済み

## 注意点

テスト行の完全削除はSupabase側で`401`になったため、行の中身を空データへ上書きした。公開前には、Supabase Auth・RLS・ユーザーごとの削除設計を確認する。

## 次回

1. スマートフォンでIncomeMateを開く
2. ホーム画面へ追加する
3. PCとスマートフォンで同じデータを確認する
